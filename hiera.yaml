---
  version: 5
  defaults:
    datadir: /tmp/vagrant-puppet/hieradata
    data_hash: yaml_data
  hierarchy:
    - name: "Per node data"
      path: "nodes/%{::fqdn}.yaml"

    - name: "Common data"
      path: "common.yaml"
